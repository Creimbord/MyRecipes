<%= render 'shared/page_title', title: @recipe.name %>

<div class="row">
  <div class="col-md-4 pull-right center">
    <%= gravatar_for @recipe.chef, size: 200 %>
    <p>
      <h5>Brought to you by: <%= @recipe.chef.chefname %></h5>
    </p>
  </div>
  <div class="col-md-8">
    <h3><%= @recipe.summary %></h3>
    <div class="well">
      <p>
        <strog>Steps:</strog>
      </p>
      <%= simple_format(@recipe.description) %>
    </div>
    <%= link_to "Edit this Recipe", edit_recipe_path(@recipe), class: "btn
    btn-success pull-right" %>
  </div>
</div>

<h5><%= link_to 'Return to Recipes Listing', recipes_path, class: "btn
btn-warning btn-small" %></h5>
